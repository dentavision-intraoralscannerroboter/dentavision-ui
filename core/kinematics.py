# for future implementations
from core.models import RobotArm

def solve_ik(x: float, y: float, z: float, rx: float, ry: float, rz: float) -> RobotArm:
    raise NotImplementedError("IK not connected")